wallpaper {
    # Empty monitor means all currently connected outputs. This works for the
    # ThinkPad's eDP-1 and the desktop's external display without host forks.
    monitor =
    path = ~/.config/hypr/night_mountains.jpg
    fit_mode = cover
}
