# Hyprland 0.55+ uses hyprland.lua from this directory.
#
# Keep this non-autogenerated placeholder because the Ubuntu 0.56 package creates
# a deprecated hyprland.conf stub during config verification when the file is
# absent. Hyprland selects hyprland.lua first, so this file is not loaded.
